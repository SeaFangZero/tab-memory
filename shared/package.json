{
  "name": "@tab-memory/shared",
  "version": "1.0.0",
  "description": "Shared types and utilities for Tab Memory",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "jest"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
